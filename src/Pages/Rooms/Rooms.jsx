import { Helmet } from "react-helmet-async";

const Rooms = () => {
    return (
        <div>
            <Helmet>
                <title>Cox's Pearl | Rooms</title>
            </Helmet>

        </div>
    );
};

export default Rooms;